# 인증 정보
shiftee_credentials.py
.env
.env.*
!env.example

# Playwright 세션
shiftee_session.json

# 디버그 스크린샷
debug_*.png

# 메일 발송 로그 (수신자·문서명 등 개인정보 포함)
email_send_log_*.csv

# 파이프라인 입출력
input/
output/

# Windows에서 실수로 생기는 예약 이름 파일(git add 불가)
nul

# Python
__pycache__/
*.pyc
*.pyo

# 구글 서비스 계정 키
credentials/
.keys/
*.json
